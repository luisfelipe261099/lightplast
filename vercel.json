{
  "buildCommand": "npm --prefix crm run build",
  "outputDirectory": "crm/dist",
  "rewrites": [
    {
      "source": "/crm/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/crm",
      "destination": "/crm.html"
    }
  ]
}
